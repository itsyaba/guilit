interface:
  display_name: "Guilit"
  short_description: "Repo-specific patterns and workflows for guilit"
  default_prompt: "Use the guilit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true